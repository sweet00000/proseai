{
  "name": "proseai-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute proseai --file=schema.sql",
    "db:init:remote": "wrangler d1 execute proseai --remote --file=schema.sql"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "jose": "^5.0.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
